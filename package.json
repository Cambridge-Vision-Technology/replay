{
  "name": "@cambridge-vision-technology/replay",
  "version": "0.0.0-development",
  "description": "Recording/playback harness for deterministic testing of external effects (LLM, HTTP, cloud APIs)",
  "type": "module",
  "scripts": {
    "build": "node -e \"console.error('Use Nix: nix build'); process.exit(1)\"",
    "test": "node -e \"console.error('Use Nix: nix flake check'); process.exit(1)\"",
    "format": "node -e \"console.error('Use Nix: nix run .#format-fix'); process.exit(1)\""
  },
  "devDependencies": {
    "@cucumber/cucumber": "^11.2.0",
    "eslint": "^9.35.0"
  },
  "dependencies": {
    "json-stable-stringify": "^1.3.0",
    "ulid": "^2.3.0",
    "ws": "^8.18.0",
    "zstd-napi": "^0.0.12"
  },
  "keywords": [
    "testing",
    "recording",
    "playback",
    "deterministic",
    "harness",
    "fixtures"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Cambridge-Vision-Technology/replay.git"
  },
  "author": "Cambridge Vision Technology",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  }
}
