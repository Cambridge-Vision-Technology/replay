#!/usr/bin/env bash
# Automatically load the Nix development environment
# Install direnv: https://direnv.net/docs/installation.html
# Then run: direnv allow

use flake

echo "Replay Nix development environment loaded"
